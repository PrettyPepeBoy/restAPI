env: "local" # local, dev, prod, понимаем где запускаем. на локальном сервере или удаленном
storage_path: "./storage/storage.db"
http_server:
  address: "localhost:8082"
  timeout: 4s #время на отправку и чтение запроса
  idle_timeout: 60s #время жизни соединения сервера с пользователем